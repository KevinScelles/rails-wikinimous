<div class="container">
<div class="title_nav">
<h1>My article</h1>
<%= link_to "New", new_article_path(@article), class: "btn btn-primary" %>
</div>
<ul class="article">
  <% @articles.each do |article| %>
    <li><label><span><%=link_to(article.title, article_path(article)) %></span></label></li>
  <% end %>
</ul>
</div>
